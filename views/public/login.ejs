<%- include('../partials/head') %>

  <div class="login-container">
    <div class="login-card">
      <div style="font-size:48px;margin-bottom:16px;">🍵</div>
      <h2>Welcome to Teh Ais</h2>
      <p class="subtitle">Sign in to your account</p>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-error"><%= error %></div>
      <% } %>

      <form method="POST" action="/login">
        <div class="form-group">
          <label class="form-label">Email Address</label>
          <input type="email" name="email" class="form-input" placeholder="your@email.com" required>
        </div>
        <div class="form-group">
          <label class="form-label">Role</label>
          <select name="role" class="form-input">
            <option value="student">Student</option>
            <option value="school">School / University</option>
            <option value="enterprise">Enterprise / Recruiter</option>
          </select>
        </div>
        <button type="submit" class="btn btn-primary w-full">Sign In</button>
      </form>

      <div class="divider">— or try a demo —</div>

      <div class="demo-links">
        <a href="/demo-login/student">🎓 Quick Login as Student</a>
        <a href="/demo-login/school">🏫 Quick Login as School</a>
        <a href="/demo-login/enterprise">🏢 Quick Login as Enterprise</a>
      </div>

      <p style="margin-top:24px;font-size:13px;color:var(--text-secondary);">
        <a href="/">← Back to Home</a>
      </p>
    </div>
  </div>

<%- include('../partials/footer') %>
